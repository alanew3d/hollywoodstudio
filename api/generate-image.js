/**
 * HOLLYWOOD STUDIO AI — api/generate-image.js
 * Vercel serverless function: POST /api/generate-image
 *
 * Currently: route handled inside api/index.js consolidated router.
 * This stub is a placeholder for when routes are split.
 * DO NOT use until api/index.js is refactored.
 */
export default async function handler(req, res) {
  res.status(307).json({ message: 'Route handled by api/index.js' });
}
