/**
 * HOLLYWOOD STUDIO AI — app.js
 * Entry point. Initializes state, router, auth listener, and renders the first view.
 * Import order: state → router → services → views
 *
 * Currently: logic lives in index.html <script>. Migration target for:
 *   - initApp()
 *   - enterApp()
 *   - exploreAsGuest()
 *   - finishAuth()
 */
