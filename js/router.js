/**
 * HOLLYWOOD STUDIO AI — router.js
 * SPA routing: go(viewName), render(), history.pushState / popstate.
 * Maps view names to view functions.
 *
 * Currently: go() and render() declared in index.html.
 * Migration: import all view functions and map them here.
 */
