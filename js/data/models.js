/**
 * HOLLYWOOD STUDIO AI — data/models.js
 * Master catalog of AI models: window.HSAI_MODELS
 *
 * Each model entry: { id, name, type, provider, status, credits, description, ... }
 * Types: 'video' | 'image' | 'audio'
 * Status: 'available' | 'beta' | 'coming_soon' | 'external'
 *
 * Currently: window.HSAI_MODELS = [...] inline in index.html.
 * Migration priority: HIGH (large static data, safe to extract)
 */
