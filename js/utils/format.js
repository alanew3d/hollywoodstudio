/**
 * HOLLYWOOD STUDIO AI — utils/format.js
 * Formatting utilities.
 *
 * Functions: formatCredits(n), formatDate(d), formatDuration(s), truncate(str, n)
 *
 * Currently: formatting inline in view functions.
 */
