/**
 * HOLLYWOOD STUDIO AI — utils/validators.js
 * Input validation helpers.
 *
 * Functions: isValidEmail(email), isValidPrompt(text), isAdminCredentials(email, pass)
 *
 * Currently: validation inline in auth and form functions.
 */
