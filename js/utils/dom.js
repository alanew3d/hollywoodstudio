/**
 * HOLLYWOOD STUDIO AI — utils/dom.js
 * DOM utility helpers.
 *
 * Functions: $id(id), $q(selector), $qa(selector), setHTML(id, html)
 * Lightweight wrappers around document.getElementById, querySelector, etc.
 *
 * Currently: direct DOM calls throughout index.html.
 */
