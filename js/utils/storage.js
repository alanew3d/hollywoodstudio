/**
 * HOLLYWOOD STUDIO AI — utils/storage.js
 * localStorage / sessionStorage helpers with error handling.
 *
 * Functions: lsGet(key, fallback), lsSet(key, val), ssGet(key), ssSet(key, val)
 * Handles: storage quota errors, private browsing fallbacks.
 *
 * Currently: direct localStorage calls throughout index.html.
 */
