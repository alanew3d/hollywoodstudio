/**
 * HOLLYWOOD STUDIO AI — components/cards.js
 * Reusable card components for models, plans, gallery items.
 *
 * Functions: modelCard(model), planCard(plan), galleryCard(item)
 *
 * Currently: card HTML generated inline in view functions.
 */
