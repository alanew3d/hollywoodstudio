/**
 * HOLLYWOOD STUDIO AI — components/topbar.js
 * Global top bar (logo, Criar rápido / Studio completo buttons, auth, user menu).
 *
 * Function: renderTopbar()
 * Shows: user avatar when logged in, login button when guest.
 *
 * Currently: topbar HTML in index.html layout function.
 */
