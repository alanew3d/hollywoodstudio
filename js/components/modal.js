/**
 * HOLLYWOOD STUDIO AI — components/modal.js
 * Reusable modal/overlay system.
 *
 * Functions: openModal(content), closeModal(), openConfirm(msg, onOk)
 * Pattern: div.overlay > div.modal-card
 * Close on: click outside, ESC key, × button, programmatic closeModal()
 *
 * Currently: modals created ad-hoc in index.html.
 */
