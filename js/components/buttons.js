/**
 * HOLLYWOOD STUDIO AI — components/buttons.js
 * Reusable button HTML generators.
 *
 * Functions: primaryBtn(label, onclick), ghostBtn(label, onclick), iconBtn(icon, label, onclick)
 * Styles: .btn-primary / .gold-button (never black on hover in dark theme)
 *
 * Currently: buttons generated inline in each view function.
 */
