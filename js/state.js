/**
 * HOLLYWOOD STUDIO AI — state.js
 * Global application state (window.S / S object).
 * Holds: S.user, S.view, S.credits, S.lang, S.theme, S.studioState
 *
 * Currently: S = { ... } declared inline in index.html.
 * Migration: export const S = { ... }; window.S = S;
 */
